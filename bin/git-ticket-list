#!/bin/sh

SUBDIRECTORY_OK=1
if ! . "`git --exec-path`/git-sh-setup"; then
	exit 1
fi
if ! cd_to_toplevel; then
	exit 1
fi

if [ -d .tickets ]; then
	cd .tickets
	grep -vil 'In-Reply-To' * | grep -v current
fi
